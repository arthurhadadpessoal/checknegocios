{
  "name": "banner-renderer",
  "version": "1.0.0",
  "description": "Microservice that renders banner HTML via Puppeteer and returns PNG",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer-core": "^22.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
